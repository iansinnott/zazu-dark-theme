{
  "name": "zazu-dark-theme",
  "version": "1.0.4",
  "description": "A dark theme for Zazu.",
  "scripts": {
    "preversion": "npm run build",
    "postversion": "git push && git push --tags",
    "bump": "npm version patch",
    "start": "stylus src/main.styl -o dist -w",
    "build": "stylus src/main.styl -o dist"
  },
  "author": "Blaine Schmeisser",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "stylus": "^0.54.5"
  }
}
